using UnityEngine;
using System.Collections;

public class MasterCanvas : MonoBehaviour {

	// Use this for initialization
	public ProjectManager pManager;
	void Start () {
//		pManager.SetPixelOffset(GetComponent<RectTransform>().position.x);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
